import { createAction } from 'redux-actions';

export const submitProfile = createAction('SUBMIT_PROFILE_FORM');
export const setProfile = createAction('SET_PROFILE'); 
export const removeProfile = createAction('REMOVE_PROFILE'); 